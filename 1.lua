local a = game.get_omt_id (g:get_cur_om(), player:global_omt_location())

print (tostring(a))

local d = game.get_omt_dir (g:get_cur_om(), player:global_omt_location())

print (tostring(d))

--local c = enums.overmap_direction

--print (tostring(c[1]))
